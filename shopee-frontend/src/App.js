// src/App.js

import './App.css'; // Mantenemos el CSS para que se vea centrado

function App() {
  // No hay estado, no hay funciones, no hay lógica.
  // Simplemente devuelve un elemento HTML.
  return (
    <div className="assistant-container">
      <h1>¡Hola, Mundo! Si puedes ver este mensaje, la aplicación base funciona.</h1>
    </div>
  );
}

export default App;